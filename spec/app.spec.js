// use this to test all CRUD requests

const expect = require('chai');

describe('CRUD tests', () => {
  describe('topics', () => {
    it('', () => {

    });
  });
  describe('users', () => {
    it('', () => {

    });
  });
  describe('articles', () => {
    it('', () => {

    });
  });
  describe('comments', () => {
    it('', () => {

    });
  });
});
